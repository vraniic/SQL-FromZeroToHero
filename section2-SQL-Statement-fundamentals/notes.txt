===========
DATABASES
===========
Databases are systems that allow users to store and organize data.

They are useful when dealing with large amounts of data.


===========
SPREADSHEETS
===========
Most users have some familiarity with spreadsheet sofrware (Excel etc...)


===========
SELECT STATEMENT
===========

SELECT <IME tabele> FROM  <IME kolone>


===========
COUNT
===========
SELECT COUNT <NAME> FROM table


RETURNS the number of input rows that match a specific condition of a query!

We can apply COUNT on a specific column or just pass COUNT(*), as we will soon see this should return the same result!

===========
WHERE
===========

SELECT * FROM customer
WHERE  <name>='IME'

POSLE where treba da ide uslov koji treba da se ispuni da bi nam sql vratio odredjenu kolonu!


===========
ORDER BY 
===========
SELECT <kolone koje nam trebaju> FROM <tabela koju gledamo>
ORDER BY <ime kolone po kojoj da se orderuje> + ASC/DESC


===========
LIMIT
===========

SELECT <kolone koje nam trebaju> FROM <tabela koju gledamo>
ORDER BY <ime kolone po kojoj da se orderuje> + ASC/DESC
LIMIT 5 --> da prikaze samo 5 most recent!

===========
BETWEEN
===========
value ARE inlucded!!!

SELECT color FROM table 
WHERE  color IN (COLORS)

SELECTOVACE REDOVE KOJI IMAJU BOJU KOJA JE NAVEDENA U ZAGRADI



===========
NOT BETWEEN
===========
values are NOT included!


===========
LIKE
===========

CASE SENSITIVE 
WHERE name LIKE 'A%' --> znaci da pocinje sa A ali se gleda iskljucivo veliko A

dok ukoliko nmaestimo seldece
WHERE name ILIKE '%a'
onda se gleda nebitno da li malo ili veliko slovo tj. case insensitive

~